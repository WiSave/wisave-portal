{
  "users": [
    {
      "name": "guest",
      "password": "guest",
      "tags": ["administrator"]
    }
  ],
  "vhosts": [
    { "name": "/" },
    { "name": "portal" },
    { "name": "expenses" },
    { "name": "incomes" },
    { "name": "stocks" }
  ],
  "permissions": [
    { "user": "guest", "vhost": "/", "configure": ".*", "write": ".*", "read": ".*" },
    { "user": "guest", "vhost": "portal", "configure": ".*", "write": ".*", "read": ".*" },
    { "user": "guest", "vhost": "expenses", "configure": ".*", "write": ".*", "read": ".*" },
    { "user": "guest", "vhost": "incomes", "configure": ".*", "write": ".*", "read": ".*" },
    { "user": "guest", "vhost": "stocks", "configure": ".*", "write": ".*", "read": ".*" }
  ]
}
