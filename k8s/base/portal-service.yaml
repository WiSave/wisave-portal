apiVersion: v1
kind: Service
metadata:
  name: portal
  namespace: wisave
  labels:
    app: portal
spec:
  type: ClusterIP
  selector:
    app: portal
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
